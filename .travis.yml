cache: pip

language: python

python:
  - "2.7"
  - "3.5"

install:
  - pip install tox-travis
  - pip install codecov
  - pip install codeclimate-test-reporter

script:
  - tox

after_success:
  - codecov
  - codeclimate-test-reporter

